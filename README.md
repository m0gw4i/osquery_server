# Server for Managing osquery
## Work in progress

Install Reqs

`pip install -r requirements.txt`

Start RethinkDB and create tables

`rethinkdb`

Start server

`python server.py`

Test osqueryd

`sh test_osqueryd.sh`
